# 更新日志

## v1.2.0 - 2026-06-06

### 新增
- 磁盘占用显示：实时显示录制文件夹大小和磁盘剩余空间
- 磁盘空间不足时自动变色提醒（<30GB 黄色，<10GB 红色）
- `启动.bat` 双击启动脚本，不需要会 Python

### 修复
- 打开录制文件夹按钮现在读取 OBS 实际配置的路径，不再打开错误目录

## v1.1.0 - 2026-06-06

### 新增
- GUI 图形界面（CustomTkinter）
- 状态指示灯：OBS 连接状态 + 录制状态
- 一键启停监控按钮
- 参数配置页面（OBS 地址/端口/密码、轮询间隔等）
- 实时日志输出
- 系统托盘支持（关闭窗口最小化到托盘）
- 打开录制文件夹按钮

### 重构
- 拆分为模块化结构：`recorder.py`（录制逻辑）、`config.py`（配置）、`gui.py`（界面）

## v1.0.0 - 2026-06-06

### 新增
- 初版命令行脚本
- 自动检测 `League of Legends.exe` 进程
- 通过 WebSocket 控制 OBS 开始/停止录制
- 状态机：空闲 → 等待 → 录制 → 冷却
- 配置文件 `config.yaml`
